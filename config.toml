logging-to-file = true        # enables logging of every patch process to a seperate file
enable-magisk-update = true   # set this to false if you do not want to receive updates for the module in magisk app
build-mindetach-module = true

# add 'enabled = false' for not patching a specific app or remove it from the config
# see https://github.com/j-hc/revanced-magisk-module/blob/main/CONFIG.md for more detailed explanations

[YouTube]
apkmirror-dlurl = "https://apkmirror.com/apk/google-inc/youtube/"
arch = "arm64-v8a"
version = "beta"
excluded-patches = "always-autorepeat copy-video-url custom-branding disable-zoom-haptics enable-debugging external-downloads hide-email-address hide-seekbar hide-timestamp premium-heading return-youtube-dislike seekbar-tapping swipe-controls tablet-mini-player theme vanced-microg-support video-speed wide-searchbar"
